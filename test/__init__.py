"""Tests for the application."""
# This is to make it possible to import modules from .. into the test/
# directory.
# It gets executed when the module is accessed.

import sys
import os
sys.path.append(os.path.dirname(os.path.realpath(__file__)) + "/../")